[project]
name = "therapist-scrapper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.13.0",
    "curl-cffi>=0.15.0",
    "pydantic>=2.0.0",
    "psycopg[binary]>=3.2.0",
    "sqlalchemy>=2.0.0",
]
